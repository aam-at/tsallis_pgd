defaults:
  - ddcatmodel
name: "pspnet_no_defense"
checkpoint: ${paths.models_dir}/pretrain_ddcat/voc2012/pspnet/no_defense/train_epoch_50.pth
model:
  _target_: models.PSPNet
  classes: ${data.info.num_classes}
  pretrained: false
