# SegPGD-style AutoPGD attack with annealed weighting
defaults:
  - auto_pgd_ce
  - override gradient_aggregator: annealed_corrects_weighted_sum
name: auto_segpgd_${.ord}_${.loss.name}_eps${fstr:${.epsilon},.1f}_step${fstr:${.stepsize},.1f}_n${.iterations}_N${.restarts}
